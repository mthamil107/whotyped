{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "whotyped.serviceAccountName" . }}
  labels:
    {{- include "whotyped.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
