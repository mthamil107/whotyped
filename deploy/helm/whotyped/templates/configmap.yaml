apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "whotyped.fullname" . }}
  labels:
    {{- include "whotyped.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
